description "Gunicorn application server running imectf"

start on runlevel [2345]
stop on runlevel [!2345]

respawn
setuid user
setgid www-data

env PATH=/srv/ctf/env/bin
chdir /srv/ctf
exec gunicorn --workers 3 --bind unix:imectf.sock -m 007 server:app
